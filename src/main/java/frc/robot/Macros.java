/*
package frc.robot;

// Although this built successfully, it might not run with the rio since the while loop is still infinite.
// To run basic drive, just nulify this whole file and run with just Main.java and Robot.java . 
// DOESN'T work with Build anymore. 

import edu.wpi.first.wpilibj.Joystick;
import edu.wpi.first.wpilibj.TimedRobot;
import edu.wpi.first.wpilibj.drive.DifferentialDrive;

import com.fasterxml.jackson.databind.jsontype.impl.StdTypeResolverBuilder;
import com.revrobotics.CANSparkMax;
import com.revrobotics.CANSparkMaxLowLevel.MotorType;

public class Macros{
    private Joystick stick;

    public int Macro; {
        boolean x = true;
        while (x = true) {
            if (stick.getRawButton(1)) {
                System.out.print("Button One was pressed!");
                //leftrearmotor.getEncoder();         
            }
        

        }
    }
}
*/